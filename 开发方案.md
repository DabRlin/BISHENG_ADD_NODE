# BISHENG 工作流节点开发方案

## 📋 目录
- [一、开发流程概述](#一开发流程概述)
- [二、前置准备工作](#二前置准备工作)
- [三、后端开发步骤](#三后端开发步骤)
- [四、前端开发步骤](#四前端开发步骤)
- [五、测试验证](#五测试验证)
- [六、开发注意事项](#六开发注意事项)
- [七、最佳实践建议](#七最佳实践建议)

---

## 一、开发流程概述

开发一个新的工作流节点需要完成以下主要步骤：

```
1. 需求分析 → 2. 后端开发 → 3. 前端开发 → 4. 联调测试 → 5. 上线部署
     ↓              ↓              ↓              ↓              ↓
  确定功能      节点逻辑实现    UI界面开发     功能验证      文档编写
```

### 开发工作量估算
- **后端开发**：2-4 天（取决于业务逻辑复杂度）
- **前端开发**：1-2 天
- **测试调试**：1-2 天
- **总计**：约 4-8 天

---

## 二、前置准备工作

### 2.1 环境准备
- [x] Python 3.8+ 环境
- [x] Node.js 16+ 环境
- [x] 熟悉项目结构
- [x] 了解现有节点实现

### 2.2 技术栈了解
**后端技术栈：**
- Python
- Pydantic（数据验证）
- LangChain/LangGraph（工作流框架）
- 异步编程（async/await）

**前端技术栈：**
- React + TypeScript
- React Flow（画布库）
- TailwindCSS（样式）
- Lucide React（图标库）

### 2.3 需求分析清单
在开发前，需要明确：
- [ ] 节点的核心功能是什么？
- [ ] 需要哪些输入参数？
- [ ] 会产生什么输出？
- [ ] 是否需要调用外部服务/API？
- [ ] 是否需要文件上传/下载？
- [ ] 是否需要流式输出？
- [ ] 是否需要特殊的UI交互？

---

## 三、后端开发步骤

### 步骤 1：定义节点类型枚举

**位置**：`src/backend/bisheng/workflow/common/node.py`

**操作**：在 `NodeType` 枚举类中添加新节点类型

```python
class NodeType(Enum):
    """ 节点类型 """
    START = "start"
    END = "end"
    # ... 其他现有节点 ...
    
    # 👇 添加你的新节点类型
    YOUR_NODE = "your_node"  # 改为你的节点类型名称
```

**命名规范**：
- 使用小写字母和下划线
- 语义清晰，见名知意
- 例如：`http_request`、`email_sender`、`data_processor`

---

### 步骤 2：创建节点实现类

**位置**：`src/backend/bisheng/workflow/nodes/your_node/`

**2.1 创建目录结构**
```bash
mkdir -p src/backend/bisheng/workflow/nodes/your_node
touch src/backend/bisheng/workflow/nodes/your_node/__init__.py
touch src/backend/bisheng/workflow/nodes/your_node/your_node.py
```

**2.2 实现节点类**

文件：`your_node.py`

```python
from typing import Any, Dict
from bisheng.workflow.nodes.base import BaseNode


class YourNode(BaseNode):
    """
    你的节点名称 - 简要描述节点功能
    """

    def __init__(self, *args, **kwargs):
        """初始化节点"""
        super().__init__(*args, **kwargs)
        
        # 从节点配置中获取参数
        # self.node_params 包含前端传入的所有参数
        self._param1 = self.node_params.get('param1', '')
        self._param2 = self.node_params.get('param2', 0)
        
        # 初始化其他必要的资源
        # 例如：API客户端、数据库连接等

    def _run(self, unique_id: str) -> Dict[str, Any]:
        """
        核心执行逻辑
        
        Args:
            unique_id: 本次执行的唯一标识
            
        Returns:
            Dict: 返回结果字典，key为输出变量名，value为变量值
                 这些输出可以被其他节点通过 node_id.key 引用
        """
        # 1. 获取其他节点的变量（如果需要）
        # input_value = self.get_other_node_variable('input_node.output_key')
        
        # 2. 执行核心业务逻辑
        try:
            result = self._execute_business_logic()
            
            # 3. 如果需要输出给用户（可选）
            # if self._output_user:
            #     self.graph_state.save_context(content=result, msg_sender='AI')
            
            # 4. 返回输出结果
            return {
                'output': result,  # 输出变量，其他节点可通过 node_id.output 引用
                # 可以返回多个输出
                # 'output2': result2,
            }
            
        except Exception as e:
            # 错误处理
            raise Exception(f"节点执行失败: {str(e)}")

    def _execute_business_logic(self) -> Any:
        """
        业务逻辑实现（可选，用于代码组织）
        """
        # 实现具体的业务逻辑
        pass

    def parse_log(self, unique_id: str, result: dict) -> Any:
        """
        解析节点执行日志（用于前端展示）
        
        Returns:
            List[List[Dict]]: 日志数据结构
            外层列表：轮次（如果节点执行多次）
            内层列表：每轮的日志条目
            Dict: 单条日志
                - key: 变量名或标签
                - value: 值
                - type: 类型 (params/variable/tool/key/file)
        """
        return [[
            {
                "key": "input_param",
                "value": self._param1,
                "type": "params"
            },
            {
                "key": f"{self.id}.output",
                "value": result.get('output', ''),
                "type": "variable"
            }
        ]]

    # 可选方法：如果需要处理用户输入
    # def handle_input(self, user_input: dict) -> Any:
    #     """处理用户输入"""
    #     self.node_params.update(user_input)

    # 可选方法：如果节点需要用户交互
    # def get_input_schema(self) -> Any:
    #     """返回需要用户输入的表单描述"""
    #     return None
```

**核心方法说明：**

| 方法 | 必需 | 说明 |
|------|------|------|
| `__init__` | ✅ 是 | 初始化节点，获取配置参数 |
| `_run` | ✅ 是 | 核心执行逻辑，返回输出结果 |
| `parse_log` | ✅ 是 | 返回执行日志，用于前端展示 |
| `handle_input` | ⚪ 否 | 处理用户输入（需要交互时） |
| `get_input_schema` | ⚪ 否 | 定义输入表单结构 |
| `is_condition_node` | ⚪ 否 | 是否为条件分支节点 |
| `route_node` | ⚪ 否 | 条件分支路由逻辑 |

**可用的基类方法：**
```python
# 获取其他节点的变量
self.get_other_node_variable('node_id.variable_name')

# 解析带变量的消息模板
self.parse_msg_with_variables('Hello {{node_id.output}}')

# 保存到聊天历史
self.graph_state.save_context(content='消息内容', msg_sender='AI')

# 设置全局变量
self.graph_state.set_variable(self.id, 'key', 'value')

# 获取全局变量
self.graph_state.get_variable('node_id', 'key')

# 回调管理器（用于前端实时更新）
self.callback_manager.on_output_msg(OutputMsgData(...))
```

---

### 步骤 3：注册节点到节点工厂

**位置**：`src/backend/bisheng/workflow/nodes/node_manage.py`

**操作**：

```python
# 1. 导入你的节点类
from bisheng.workflow.nodes.your_node.your_node import YourNode

# 2. 在 NODE_CLASS_MAP 中注册
NODE_CLASS_MAP = {
    NodeType.START.value: StartNode,
    NodeType.END.value: EndNode,
    # ... 其他节点 ...
    
    # 👇 添加你的节点
    NodeType.YOUR_NODE.value: YourNode,
}
```

---

### 步骤 4：（可选）添加节点到默认配置

**位置**：`src/backend/bisheng/default_node.yaml`

如果你的节点属于某个分类（如 tools、chains 等），可以添加到该文件中：

```yaml
your_category:
  YourNode:
    documentation: "https://your-docs-url"
```

---

## 四、前端开发步骤

### 步骤 1：添加节点图标和颜色

**位置**：`src/frontend/platform/src/pages/BuildPage/flow/FlowNode/NodeLogo.tsx`

**操作**：

```typescript
import { 
    BookOpenTextIcon, Bot, Brain, Code2, 
    FileDown, FileSearch, FlagTriangleRight, 
    Hammer, Home, Keyboard, MessagesSquareIcon, Split,
    // 👇 导入你的图标（从 lucide-react）
    YourIcon  // 例如：Database, Mail, Globe, etc.
} from "lucide-react";

export const Icons = {
    'start': Home,
    'input': Keyboard,
    // ... 其他节点 ...
    
    // 👇 添加你的节点图标
    'your_node': YourIcon,
}

export const Colors = {
    'start': 'bg-[#FFD89A]',
    'input': 'bg-primary',
    // ... 其他节点 ...
    
    // 👇 添加你的节点颜色
    'your_node': 'bg-[#YOUR_COLOR]',  // 例如：bg-[#A5D8FF]
}
```

**颜色选择建议**：
- 选择柔和的浅色系
- 避免与现有节点颜色冲突
- 可参考现有节点的配色方案

**图标选择建议**：
- 访问 [Lucide Icons](https://lucide.dev/icons/) 选择合适的图标
- 选择语义清晰的图标
- 尺寸统一为 14px

---

### 步骤 2：创建节点模板配置

**位置**：`src/frontend/platform/src/controllers/API/workflow.ts`

**操作**：在 `workflowTemplate` 数组中添加节点模板

```typescript
const workflowTemplate = [
    {
        "id": "start_xxx",
        "name": "开始",
        // ... 其他节点 ...
    },
    // 👇 添加你的节点模板
    {
        "id": "your_node_xxx",  // 节点ID，会被前端自动生成唯一ID
        "name": "你的节点名称",  // 显示名称
        "description": "节点功能描述",  // 节点描述
        "type": "your_node",  // 节点类型，对应后端的 NodeType
        "v": "1",  // 版本号
        "group_params": [  // 参数分组
            {
                "name": "基础配置",  // 分组名称
                "params": [
                    {
                        "key": "param1",  // 参数key，对应后端的 node_params
                        "label": "参数1",  // 参数显示名称
                        "value": "",  // 默认值
                        "type": "input",  // 参数类型
                        "placeholder": "请输入参数1",
                        "required": true,  // 是否必填
                        "help": "参数说明文本"  // 帮助提示
                    },
                    {
                        "key": "param2",
                        "label": "参数2",
                        "value": 0,
                        "type": "number",
                        "placeholder": "请输入数字",
                        "required": false
                    }
                ]
            }
        ]
    }
]
```

**常用参数类型（type 字段）：**

| 类型 | 说明 | 示例配置 |
|------|------|---------|
| `input` | 单行文本输入 | `{"type": "input", "value": ""}` |
| `textarea` | 多行文本输入 | `{"type": "textarea", "value": ""}` |
| `number` | 数字输入 | `{"type": "number", "value": 0}` |
| `select` | 下拉选择 | `{"type": "select", "value": "", "options": [{key: "", label: ""}]}` |
| `checkbox` | 复选框 | `{"type": "checkbox", "value": false}` |
| `file` | 文件上传 | `{"type": "file", "value": ""}` |
| `variable` | 变量选择器 | `{"type": "variable", "value": ""}` |
| `model` | 模型选择器 | `{"type": "model", "value": ""}` |
| `knowledge` | 知识库选择器 | `{"type": "knowledge", "value": []}` |
| `tool` | 工具选择器 | `{"type": "tool", "value": []}` |

**高级配置示例：**

```typescript
// 带 Tab 切换的配置
{
    "tab": {
        "value": "tab1",  // 当前tab
        "options": [
            {
                "label": "选项卡1",
                "key": "tab1",
                "help": "选项卡说明"
            },
            {
                "label": "选项卡2",
                "key": "tab2"
            }
        ]
    },
    "group_params": [
        {
            "name": "配置组",
            "params": [
                {
                    "key": "param_in_tab1",
                    "tab": "tab1",  // 只在 tab1 显示
                    // ... 其他配置
                }
            ]
        }
    ]
}

// 变量引用示例
{
    "key": "input_variable",
    "label": "输入变量",
    "value": "",
    "type": "variable",  // 变量选择器
    "placeholder": "选择其他节点的输出变量",
    "help": "可以引用其他节点的输出，格式：node_id.output_key"
}

// 模型选择示例
{
    "key": "model_id",
    "label": "选择模型",
    "value": "",
    "type": "model",
    "required": true,
    "help": "选择用于推理的大语言模型"
}
```

---

### 步骤 3：（可选）自定义节点UI组件

如果默认的节点UI不满足需求，可以自定义节点组件。

**位置**：`src/frontend/platform/src/pages/BuildPage/flow/FlowNode/index.tsx`

大多数情况下，使用默认的 `CustomNode` 组件即可，它会根据节点模板自动渲染UI。

---

### 步骤 4：类型定义（可选但推荐）

**位置**：`src/frontend/platform/src/types/flow/index.ts`

如果需要严格的类型检查，可以扩展 `WorkflowNode` 接口：

```typescript
export interface WorkflowNode {
  id: string;
  name: string;
  description: string;
  type: string; // 'start' | 'output' | ... | 'your_node';
  group_params: {
    name?: string;
    params: WorkflowNodeParam[];
  }[];
  // ... 其他字段
}
```

---

## 五、测试验证

### 5.1 后端单元测试

创建测试文件：`src/backend/test/test_your_node.py`

```python
import pytest
from bisheng.workflow.nodes.your_node.your_node import YourNode
from bisheng.workflow.common.node import BaseNodeData


def test_your_node_basic():
    """测试节点基本功能"""
    # 准备测试数据
    node_data = BaseNodeData(
        id="test_node",
        type="your_node",
        name="测试节点",
        group_params=[{
            "params": [
                {"key": "param1", "value": "test_value"}
            ]
        }]
    )
    
    # 创建节点实例
    node = YourNode(
        node_data=node_data,
        workflow_id="test_workflow",
        user_id="test_user",
        graph_state=None,
        target_edges=[],
        max_steps=100,
        callback=None
    )
    
    # 执行测试
    result = node._run("test_unique_id")
    
    # 断言结果
    assert "output" in result
    assert result["output"] is not None


def test_your_node_error_handling():
    """测试错误处理"""
    # 测试异常情况
    pass
```

运行测试：
```bash
cd src/backend
pytest test/test_your_node.py -v
```

---

### 5.2 前端功能测试

**测试清单：**
- [ ] 节点能否正常拖拽到画布
- [ ] 节点图标和颜色是否正确显示
- [ ] 点击节点能否打开配置面板
- [ ] 参数输入框是否正常工作
- [ ] 变量引用是否能正确选择其他节点的输出
- [ ] 节点连线是否正常
- [ ] 保存工作流是否成功

---

### 5.3 端到端测试

**创建测试工作流：**

```
[开始节点] → [输入节点] → [你的节点] → [输出节点] → [结束节点]
```

**测试步骤：**
1. 启动后端服务：`cd src/backend && python -m bisheng.main`
2. 启动前端服务：`cd src/frontend/platform && npm run dev`
3. 在浏览器中创建新的工作流
4. 添加你的节点到画布
5. 配置节点参数
6. 连接节点
7. 保存工作流
8. 运行工作流
9. 查看执行日志和结果

**验证项：**
- [ ] 节点能正常执行
- [ ] 参数传递正确
- [ ] 输出结果符合预期
- [ ] 执行日志完整
- [ ] 错误处理正常
- [ ] 性能表现良好

---

## 六、开发注意事项

### 6.1 代码规范

**Python 代码规范：**
- 遵循 PEP 8 规范
- 使用类型提示（Type Hints）
- 添加完整的文档字符串（Docstring）
- 异常处理要完善
- 日志记录要详细

**TypeScript 代码规范：**
- 使用严格的类型定义
- 组件命名使用 PascalCase
- 函数命名使用 camelCase
- 添加必要的注释

---

### 6.2 性能优化

**后端性能优化：**
- 避免在 `__init__` 中执行耗时操作
- 大数据量处理考虑分批处理
- 合理使用缓存
- 数据库查询要优化
- 考虑使用异步 IO（`async/await`）

**前端性能优化：**
- 避免不必要的组件重渲染
- 大列表使用虚拟滚动
- 图片懒加载
- 防抖和节流

---

### 6.3 安全考虑

- [ ] 输入参数要验证和过滤
- [ ] 防止 SQL 注入
- [ ] 防止命令注入（如果执行外部命令）
- [ ] API 调用要鉴权
- [ ] 敏感信息不要记录到日志
- [ ] 文件上传要限制类型和大小

---

### 6.4 错误处理

**错误处理最佳实践：**

```python
def _run(self, unique_id: str):
    try:
        # 业务逻辑
        result = self._execute()
        return {"output": result}
        
    except ValueError as e:
        # 参数错误
        raise Exception(f"参数错误: {str(e)}")
        
    except ConnectionError as e:
        # 网络错误
        raise Exception(f"网络连接失败: {str(e)}")
        
    except Exception as e:
        # 其他错误
        logger.error(f"节点执行失败: {str(e)}", exc_info=True)
        raise Exception(f"节点执行失败: {str(e)}")
```

---

### 6.5 日志记录

**使用 loguru 记录日志：**

```python
from loguru import logger

# 调试信息
logger.debug(f"节点参数: {self.node_params}")

# 一般信息
logger.info(f"开始执行节点: {self.name}")

# 警告信息
logger.warning(f"参数缺失，使用默认值")

# 错误信息
logger.error(f"执行失败: {str(e)}")
```

---

## 七、最佳实践建议

### 7.1 参考现有节点

**建议参考的节点：**

| 如果你要开发... | 推荐参考 | 文件位置 |
|----------------|---------|---------|
| 简单的数据处理节点 | CodeNode | `workflow/nodes/code/code.py` |
| 调用外部 API | ToolNode | `workflow/nodes/tool/tool.py` |
| LLM 调用相关 | LLMNode | `workflow/nodes/llm/llm.py` |
| 需要流式输出 | AgentNode | `workflow/nodes/agent/agent.py` |
| 条件分支逻辑 | ConditionNode | `workflow/nodes/condition/condition.py` |
| 文件处理 | InputNode | `workflow/nodes/input/input.py` |
| 复杂的输出 | ReportNode | `workflow/nodes/report/report.py` |

---

### 7.2 开发建议

**渐进式开发：**
1. 先实现最小可用版本（MVP）
2. 测试基本功能
3. 逐步添加高级特性
4. 持续优化和重构

**代码复用：**
- 使用基类提供的工具方法
- 提取公共逻辑到工具函数
- 参考现有节点的实现模式

**文档先行：**
- 先写清楚节点的功能说明
- 定义好输入输出格式
- 画出数据流图
- 然后再开始编码

---

### 7.3 常见问题处理

**问题 1：节点执行报错 "未知的节点类型"**
- 检查是否在 `node_manage.py` 中注册了节点
- 检查节点类型名称是否一致

**问题 2：前端看不到节点**
- 检查是否在 `workflowTemplate` 中添加了模板
- 检查浏览器控制台是否有错误
- 清除浏览器缓存

**问题 3：参数无法传递到后端**
- 检查前端的 `key` 是否与后端的参数名一致
- 检查参数序列化是否正确

**问题 4：变量引用报错**
- 检查引用的节点是否在当前节点之前执行
- 检查变量名格式是否正确（`node_id.variable_name`）

**问题 5：节点图标不显示**
- 检查是否正确导入了图标组件
- 检查图标名称拼写是否正确

---

## 八、开发检查清单

### 后端开发 ✅
- [x] 在 `NodeType` 枚举中添加节点类型
- [x] 创建节点实现类，继承 `BaseNode`
- [ ] 实现 `__init__` 方法
- [ ] 实现 `_run` 方法（核心逻辑）
- [ ] 实现 `parse_log` 方法
- [ ] 在 `node_manage.py` 中注册节点
- [ ] 添加完整的错误处理
- [ ] 添加日志记录
- [ ] 编写单元测试

### 前端开发 ✅
- [ ] 在 `NodeLogo.tsx` 中添加图标和颜色
- [ ] 在 `workflow.ts` 中添加节点模板
- [ ] 配置所有必需的参数
- [ ] 设置参数的类型和默认值
- [ ] 添加参数说明和帮助文本
- [ ] 测试节点拖拽和配置

### 测试验证 ✅
- [ ] 后端单元测试通过
- [ ] 前端功能测试通过
- [ ] 端到端测试通过
- [ ] 边界情况测试
- [ ] 错误处理测试
- [ ] 性能测试

### 文档编写 ✅
- [ ] 节点功能说明
- [ ] 参数说明文档
- [ ] 使用示例
- [ ] 注意事项
- [ ] 常见问题（FAQ）

---

## 九、示例：开发一个 HTTP 请求节点

### 需求描述
开发一个发送 HTTP 请求的节点，支持 GET/POST/PUT/DELETE 方法。

### 实现步骤

**1. 后端实现**

```python
# src/backend/bisheng/workflow/nodes/http_request/http_request.py
import requests
from typing import Any, Dict
from bisheng.workflow.nodes.base import BaseNode
from loguru import logger


class HttpRequestNode(BaseNode):
    """HTTP 请求节点 - 发送 HTTP 请求并返回响应"""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        
        self._url = self.node_params.get('url', '')
        self._method = self.node_params.get('method', 'GET')
        self._headers = self.node_params.get('headers', {})
        self._body = self.node_params.get('body', '')
        self._timeout = self.node_params.get('timeout', 30)

    def _run(self, unique_id: str) -> Dict[str, Any]:
        """执行 HTTP 请求"""
        # 解析 URL 中的变量
        url, _ = self.parse_msg_with_variables(self._url)
        
        # 解析 body 中的变量
        body, _ = self.parse_msg_with_variables(self._body)
        
        try:
            logger.info(f"发送 {self._method} 请求到 {url}")
            
            # 发送请求
            response = requests.request(
                method=self._method,
                url=url,
                headers=self._headers,
                data=body if body else None,
                timeout=self._timeout
            )
            
            # 检查响应
            response.raise_for_status()
            
            # 尝试解析 JSON
            try:
                result = response.json()
            except:
                result = response.text
            
            return {
                'response': result,
                'status_code': response.status_code,
                'headers': dict(response.headers)
            }
            
        except requests.Timeout:
            raise Exception(f"请求超时 (>{self._timeout}s)")
        except requests.RequestException as e:
            raise Exception(f"请求失败: {str(e)}")

    def parse_log(self, unique_id: str, result: dict) -> Any:
        """返回执行日志"""
        return [[
            {"key": "url", "value": self._url, "type": "params"},
            {"key": "method", "value": self._method, "type": "params"},
            {"key": "status_code", "value": result.get('status_code'), "type": "params"},
            {"key": f"{self.id}.response", "value": result.get('response'), "type": "variable"}
        ]]
```

**2. 注册节点**

```python
# src/backend/bisheng/workflow/common/node.py
class NodeType(Enum):
    # ...
    HTTP_REQUEST = "http_request"

# src/backend/bisheng/workflow/nodes/node_manage.py
from bisheng.workflow.nodes.http_request.http_request import HttpRequestNode

NODE_CLASS_MAP = {
    # ...
    NodeType.HTTP_REQUEST.value: HttpRequestNode,
}
```

**3. 前端配置**

```typescript
// src/frontend/platform/src/pages/BuildPage/flow/FlowNode/NodeLogo.tsx
import { Globe } from "lucide-react";

export const Icons = {
    // ...
    'http_request': Globe,
}
export const Colors = {
    // ...
    'http_request': 'bg-[#A5D8FF]',
}

// src/frontend/platform/src/controllers/API/workflow.ts
{
    "id": "http_request_xxx",
    "name": "HTTP请求",
    "description": "发送HTTP请求到指定URL",
    "type": "http_request",
    "v": "1",
    "group_params": [
        {
            "name": "请求配置",
            "params": [
                {
                    "key": "method",
                    "label": "请求方法",
                    "value": "GET",
                    "type": "select",
                    "options": [
                        {"key": "GET", "label": "GET"},
                        {"key": "POST", "label": "POST"},
                        {"key": "PUT", "label": "PUT"},
                        {"key": "DELETE", "label": "DELETE"}
                    ],
                    "required": true
                },
                {
                    "key": "url",
                    "label": "请求URL",
                    "value": "",
                    "type": "input",
                    "placeholder": "https://api.example.com/data",
                    "required": true,
                    "help": "支持变量引用，如：https://api.com/{{node_id.param}}"
                },
                {
                    "key": "body",
                    "label": "请求体",
                    "value": "",
                    "type": "textarea",
                    "placeholder": "JSON格式的请求体",
                    "help": "POST/PUT请求的请求体内容"
                },
                {
                    "key": "timeout",
                    "label": "超时时间(秒)",
                    "value": 30,
                    "type": "number",
                    "placeholder": "30"
                }
            ]
        }
    ]
}
```

---

## 十、总结

开发一个工作流节点主要包括：

1. **后端**：定义节点类型 → 实现节点逻辑 → 注册节点
2. **前端**：添加图标颜色 → 配置节点模板
3. **测试**：单元测试 → 功能测试 → 端到端测试

**关键要点：**
- 📝 充分理解需求，先设计后编码
- 🔍 参考现有节点实现
- 🧪 完善的测试和错误处理
- 📚 清晰的文档和注释
- ⚡ 关注性能和安全性

**开发流程：**
```
需求分析 → 设计方案 → 后端开发 → 前端开发 → 
联调测试 → 性能优化 → 文档编写 → 代码审查 → 上线部署
```

祝开发顺利！🚀

---

## 附录：常用资源链接

- **LangChain 文档**：https://python.langchain.com/
- **LangGraph 文档**：https://langchain-ai.github.io/langgraph/
- **React Flow 文档**：https://reactflow.dev/
- **Lucide Icons**：https://lucide.dev/icons/
- **TailwindCSS**：https://tailwindcss.com/
- **Pydantic**：https://docs.pydantic.dev/

---

**版本信息**
- 文档版本：v1.0
- 最后更新：2025-01
- 适用项目：BISHENG 工作流平台

